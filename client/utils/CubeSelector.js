import React from "react";


// Handles the Redux state
export const CubeSelector = () => {
}

<Link to={{
    pathname: "/charts",
    xAxis: time,
    yAxis: { stat1: "obj with stats"},
    zAxis: { org1: "Object with orgs"},

}}
></Link>

{
    /* Link works instead of just LinkContainer - 
    Alters global state -
    pathname could be a dynamic parameter 
    eg. "./:someName"
*/
}

initialState = {

}

  const handleClick = (e) => {};